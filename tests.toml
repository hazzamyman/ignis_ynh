#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/tests.v1.schema.json

test_format = 1.0

[default]

    # Ignis serves its API, assets and WebSocket from root-absolute paths, so it only
    # supports installation at a domain root — exclude the subpath install test.
    exclude = ["install.subdir"]
